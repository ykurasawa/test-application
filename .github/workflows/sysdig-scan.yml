name: Sysdig Secure Inline Scan

on: [push, pull_request]

# Securityタブへの報告権限をトップレベルで定義
permissions:
  contents: read
  security-events: write

jobs:
  build-and-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        # image_3efed3.png で修正した大文字の Dockerfile をビルド
        run: docker build -t my-repo/my-image:latest .

      - name: Sysdig Secure Inline Scan
        uses: sysdiglabs/scan-action@v5
        with:
          sysdig-secure-token: ${{ secrets.SYSDIG_SECURE_TOKEN }}
          sysdig-secure-url: ${{ secrets.SYSDIG_SECURE_URL }}
          image-tag: "my-repo/my-image:latest"
          # ▼【修正ポイント1】v5ではパラメータをここにまとめます
          extra-parameters: --output-format=sarif --output-path=sysdig-results.sarif
          # ▼【修正ポイント2】脆弱性があってもエラーにせず続行させるためのv5用設定
          stop-on-failed-policy-eval: false

      - name: Upload SARIF report
        uses: github/codeql-action/upload-sarif@v3
        if: always() # 前のステップが失敗しても必ず実行
        with:
          sarif_file: sysdig-results.sarif
